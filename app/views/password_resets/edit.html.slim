= simple_form_for @form, as: :password_reset_form, url: password_reset_path(token: @token), method: :put do |f|
  .card.card-shadowed
    h5.card-header
      = t(".header")
    .card-body
      = render "alerts"
      = f.input :password, autofocus: true, as: :viewable_password
    .card-footer
      => f.submit t(".submit"), class: "btn btn-primary"
      => link_to t(".cancel"), new_session_path, class: "btn btn-default"
